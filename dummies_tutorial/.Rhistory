library(dummies)
dat <- data("iris")
head(dat)
dat <- data(iris)
head(dat)
dat <- iris
head(dat)
class(dat$Species)
dummied_dat <- dummies::dummy.data.frame(dat)
dummied_dat <- dummies::dummy.data.frame(dat, sep = "_")
# one-hot encoding
dummied_dat <- dummies::dummy.data.frame(dat,                   # one-hotにしたいDataFrame
sep = "_",             # 変数名の加工．ここでは「元変数名_カテゴリ番号」という形を指定．
dummy.classes = "ALL", # classを指定してone-hot．全部やってほしいときは"ALL"
omit.constants = "Sepal_Length"
)
# one-hot encoding
dummied_dat <- dummies::dummy.data.frame(dat,                   # one-hotにしたいDataFrame
sep = "_",             # 変数名の加工．ここでは「元変数名_カテゴリ番号」という形を指定．
dummy.classes = "ALL"  # classを指定してone-hot．全部やってほしいときは"ALL"
)
head(dat)
head(dummied_dat)
# one-hot encoding
dummied_dat <- dummies::dummy.data.frame(dat,                   # one-hotにしたいDataFrame
sep = "_",             # 変数名の加工．ここでは「元変数名_カテゴリ番号」という形を指定．
dummy.classes = "factor"  # classを指定してone-hot．全部やってほしいときは"ALL"
)
head(dummied_dat)
head(dummied_dat)
dummied_dat <- dummies::dummy.data.frame(dat, sep = "_", names = "Sepal.Length")
head(dummied_dat)
dummied_dat <- dummies::dummy.data.frame(dat, sep = "_", names = c("Sepal.Length", Species))
dummied_dat <- dummies::dummy.data.frame(dat, sep = "_", names = c("Sepal.Length", "Species"))
head(dummied_dat)
?dummy.data.frame
